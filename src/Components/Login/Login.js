import React from 'react';
import Header from '../Header/Header';

const Login = () => {
    return (
        <div>
            <Header></Header>
            <h1>This is Login Page.</h1>
        </div>
    );
};

export default Login;